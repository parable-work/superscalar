SuperScalar: cross-language scalar types library (parse, normalize, validate); one Rust core with Go, Python, TypeScript and WASM bindings.
The full tree arrives with the initial import pull request from branch chore/initial-import.
